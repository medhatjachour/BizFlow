# The two apps pin different majors of shared ecosystems (React 18 vs 19,
# Tailwind 3 vs 4, Vite vs Next). Relax peer-dependency resolution so a single
# hoisted install across workspaces does not fail on cross-app peer mismatches.
# Each workspace still resolves its own pinned versions via nested node_modules.
legacy-peer-deps=true
