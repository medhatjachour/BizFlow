services:
  # Next.js website + HTTP bridge + Web BizFlow UI
  bizflow-app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_BASE_PATH: ""
        NEXT_PUBLIC_SITE_URL: https://www.bizflow.medhatjachour.tech
        ADMIN_PASSWORD: build-validation-only
        LICENSE_SECRET: build-validation-only
    container_name: bizflow-app
    ports:
      - "3000:3000"    # Nebula website
      - "5180:5180"    # BizFlow web UI
      - "8787:8787"    # HTTP bridge (IPC handlers)
    volumes:
      # Persistent database volume
      - bizflow-data:/data/bizflow
      # Development mode: mount source for hot reload (optional)
      # - ./apps/bizflow:/app/apps/bizflow
      # - ./apps/nebula:/app/apps/nebula
    environment:
      NODE_ENV: production
      DATABASE_URL: file:/data/bizflow/database.db
      WEBSITE_DATABASE_URL: file:/data/bizflow/website.db
      BIZFLOW_DATA_DIR: /data/bizflow
      BRIDGE_PORT: 8787
      NEXT_PUBLIC_BASE_PATH: ""
      NEXT_PUBLIC_SITE_URL: https://www.bizflow.medhatjachour.tech
      NEXTAUTH_URL: https://www.bizflow.medhatjachour.tech
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:?ADMIN_PASSWORD must be set in .env}
      LICENSE_SECRET: ${LICENSE_SECRET:?LICENSE_SECRET must be set in .env}
      LICENSE_SIGNING_PRIVATE_KEY: ${LICENSE_SIGNING_PRIVATE_KEY:?LICENSE_SIGNING_PRIVATE_KEY must be set in .env}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      REQUEST_MAIL_TO: medhatjachour8@gmail.com
      REQUEST_MAIL_FROM: ${REQUEST_MAIL_FROM:-medhatjachour8@gmail.com}
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_SECURE: ${SMTP_SECURE:-false}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASS: ${SMTP_PASS:-}
      REQUEST_MAIL_FALLBACK_DIRECT: ${REQUEST_MAIL_FALLBACK_DIRECT:-true}
      REQUEST_MAIL_HELO_NAME: ${REQUEST_MAIL_HELO_NAME:-medhatjachour.tech}
      REQUEST_MAIL_FALLBACK_HOST: ${REQUEST_MAIL_FALLBACK_HOST:-gmail-smtp-in.l.google.com}
      REQUEST_MAIL_FALLBACK_PORT: ${REQUEST_MAIL_FALLBACK_PORT:-25}
      # Add other env vars as needed
    restart: unless-stopped
    mem_limit: 3g
    cpus: 2.0
    healthcheck:
      test: ["CMD", "wget", "--spider", "--quiet", "http://127.0.0.1:3000/api/status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"
    networks:
      - bizflow-network

  # Optional: Nginx reverse proxy for single port access
  nginx:
    image: nginx:alpine
    container_name: bizflow-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
      - ./portfolio-dist:/usr/share/nginx/html:ro
    depends_on:
      - bizflow-app
    restart: unless-stopped
    mem_limit: 256m
    cpus: 0.5
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"
    networks:
      - bizflow-network
      - transhub-default

  dozzle:
    image: amir20/dozzle:latest
    container_name: bizflow-dozzle
    ports:
      - "127.0.0.1:8088:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: unless-stopped
    mem_limit: 128m
    cpus: 0.25
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  bizflow-data:
    driver: local

networks:
  bizflow-network:
    driver: bridge
  transhub-default:
    external: true
    name: transhub_default
